Phase 0 placeholder system prompt for source_reading. Replace with real reader instructions.

On-disk structured output for real runs should validate as `source_reading_result` (`schemas/json/source_reading_result.schema.json`); the Phase 0 skeleton emits a deterministic stub document.
