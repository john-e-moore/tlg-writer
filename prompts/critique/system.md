Phase 0 placeholder system prompt for critique.

On-disk structured output for real runs should validate as `critique_result` (`schemas/json/critique_result.schema.json`); the Phase 0 skeleton emits a deterministic stub document.
