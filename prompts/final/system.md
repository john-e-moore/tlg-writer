Phase 0 placeholder system prompt for final packaging.

On-disk structured output for real runs should validate as `final_deliverable` (`schemas/json/final_deliverable.schema.json`); the Phase 0 skeleton emits a deterministic stub document aligned with `final/piece.md`.
