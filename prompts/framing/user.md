The context above is authoritative (assigned topic, `source_reading_result`, `topic_selection_result`).

Return **only** a JSON object that validates as `framing_decision` v1 (see repository schema `schemas/json/framing_decision.schema.json`).

Do not add commentary before or after the JSON.
