Phase 0 placeholder system prompt for revision.

On-disk structured output for real runs should validate as `revision_result` (`schemas/json/revision_result.schema.json`); the Phase 0 skeleton emits a deterministic stub document.
