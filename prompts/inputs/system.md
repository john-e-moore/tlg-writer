Phase 0 placeholder system prompt for inputs (topic intake).

On-disk structured output for real runs should validate as `inputs_result` (`schemas/json/inputs_result.schema.json`); the Phase 0 skeleton emits a deterministic stub document for **assigned** mode.
