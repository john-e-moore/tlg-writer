Phase 0 placeholder system prompt for drafting.

On-disk structured output for real runs should validate as `draft_result` (`schemas/json/draft_result.schema.json`); the Phase 0 skeleton emits a deterministic stub document.
