Phase 0 placeholder system prompt for evaluation.

On-disk structured output for real runs should validate as `evaluation_result` (`schemas/json/evaluation_result.schema.json`); the Phase 0 skeleton emits a deterministic stub document.
