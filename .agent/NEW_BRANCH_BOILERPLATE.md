tlg-writer (infra and contracts phase): infer the next mergeable slice from `.agent/AGENTS.md` (including **Infra and contracts phase: auto-merge and when to stop**), `.agent/SPEC.md`, `.agent/PROGRESS.md`, and `.agent/PLANS.md` (status + Progress). Create `feature/<short-slug>` from up-to-date `main`, create an implementation plan in Cursor plan mode, implement end-to-end per those docs (feature brief + ExecPlan updates when AGENTS says so), commit in logical chunks, run the validation mix from AGENTS **Before “done”** / the ExecPlan, open a PR with body aligned to `.agent/PR_TEMPLATE.md`, and wait until GitHub Actions **Tests** (`.github/workflows/ci.yml`) is green.

Then merge the PR (prefer `gh pr merge <n> --auto --squash --delete-branch` if available and permitted; otherwise enable auto-merge in the GitHub UI). `git checkout main && git pull`, and **repeat** for the next mergeable slice until a **pause trigger** in AGENTS applies (e.g. the human is expected to inspect real run output under `artifacts/runs/…` or corpus outputs under `data/processed/pieces/`, live/paid API work, or explicit review-before-merge / blocked plan).

Only stop to ask if you’re blocked (missing decision, credentials, `gh`/merge permissions, or genuinely ambiguous next step after checking PLANS). When you pause for human review, say what to open on disk and do not merge or start the next slice until the user continues.
