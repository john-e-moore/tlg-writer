tlg-writer: infer the next mergeable slice from .agent/AGENTS.md, .agent/SPEC.md, and .agent/PLANS.md (status + Progress). Create `feature/<short-slug>` from `main`, implement it end-to-end per those docs (brief + ExecPlan updates if AGENTS says so), commit in logical chunks, run the validation mix from AGENTS “Before done” / the ExecPlan, open the PR from .agent/PR_TEMPLATE.md, then tell me the PR URL. Only stop to ask if you’re blocked (missing decision, credentials, or genuinely ambiguous next step after checking PLANS).