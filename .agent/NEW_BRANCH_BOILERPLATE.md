tlg-writer (infra and contracts phase): infer the next mergeable slice from `.agent/AGENTS.md` (including **Infra and contracts phase: auto-merge and when to stop**), `.agent/SPEC.md`, `.agent/PROGRESS.md`, and `.agent/PLANS.md` (status + Progress). Create `feature/<short-slug>` from up-to-date `main`, create an implementation plan in Cursor plan mode, implement end-to-end per those docs (feature brief + ExecPlan updates when AGENTS says so), commit in logical chunks, run the validation mix from AGENTS **Before “done”** / the ExecPlan, open a PR with body aligned to `.agent/PR_TEMPLATE.md`, and wait until GitHub Actions **Tests** (`.github/workflows/ci.yml`) is green.

Then merge the PR (prefer `gh pr merge <n> --auto --squash --delete-branch` if available and permitted; otherwise enable auto-merge in the GitHub UI). `git checkout main && git pull`, **run** the pipeline or scripts the slice implies (stub, fixture, or env-gated live per scope), and **repeat** for the next mergeable slice.

**Pause** only when **`.agent/AGENTS.md`** says so: typically after **artifacts exist on disk** for you to inspect (e.g. `artifacts/runs/<run_id>/`, `data/processed/pieces/…`)—list exact paths—or when truly **blocked** (credentials, permissions, `blocked` plan, explicit review-before-merge on a PR, missing decision). Do **not** pause early just because the roadmap mentions “human review”; merge, run, materialize outputs, **then** pause with paths. When pausing, do not start the next unrelated slice until you continue.
